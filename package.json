{
  "name": "warp-postgres-example",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "esbuild_devel": "esbuild --bundle js/index.js '--define:process.env.NODE_ENV=\"development\"' --outfile=dist/index.js --sourcemap",
    "build": "coffee -cb -o js coffee_src && npm run esbuild_devel"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mithril": "^2.0.4"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1",
    "esbuild": "^0.1.14"
  }
}
